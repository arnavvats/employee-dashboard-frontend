import {environment} from '../../../environments/environment';

export const CONSTANTS = {
  photo: environment.baseUrl + 'photo',
  employee: environment.baseUrl + 'employee',
  employeeList: environment.baseUrl + 'employee-list',
  skills: environment.baseUrl + 'skills'
};
